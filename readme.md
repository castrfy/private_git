# private_git [IN DEVELOPMENT]
A Homemade, Fully Private and Open Source Version of GitHub

# Requirements

- `OpenSSL` 3.0.0 or higher versions
- `.env` file that has `SERVER_URL` variable defined with url of the server inside itself. [exm: `SERVER_URL = "https://example.com"`]
